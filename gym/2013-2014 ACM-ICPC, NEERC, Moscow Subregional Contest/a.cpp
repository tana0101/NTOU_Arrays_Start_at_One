#include<bits/stdc++.h>
#define io ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
using namespace std;

signed main(){
    io
    int n,key;
    cin>>n;
    map<int,int>mp;
    for(int i=0;i<n;i++){
        cin>>key;
        mp[key]++;
    }

}